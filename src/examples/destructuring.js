const frameworks = {
    php: 'Lavarel',
    js: 'React',
    java: 'Maven'
};

// const {php, js} = frameworks;

// console.log(js);

// console.log({...frameworks});

module.exports = { frameworks }; //exporting module